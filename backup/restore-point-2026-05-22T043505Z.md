# Restore point — budget-consolidate (production + persistence)

| Field | Value |
|-------|--------|
| **Created** | 2026-05-22T04:35:05Z |
| **Project** | `budget-consolidate` |
| **Phase** | Production Vite app (Variant A) with **JSON file persistence** |
| **Prior restores** | [restore-point-2026-05-21.md](./restore-point-2026-05-21.md) (spec only) · [restore-point-2026-05-21T115542Z.md](./restore-point-2026-05-21T115542Z.md) (post-prototype) |

## Status at this point

| Done | Not done |
|------|----------|
| Hero-pot UI at repo root (`src/App.tsx`) | Auth / multi-user |
| `PRODUCT.md` rules (7k LKR, 98% warn, allocation-only) | Deploy / hosting |
| Variant A promoted; B/C + switcher removed | `prototype/ui` deleted |
| **`data/state.json`** load/save via `/api/state` | Static `dist/` without server (needs `npm run dev` or `preview`) |
| Auto-save (400ms debounce) + load on startup | |

## Locked decisions

- Allocation: `headcount × 7_000 LKR`
- Near-limit: **≥ 98%**; full pot OK; over = danger + overflow meter
- Warn only (never block edits)
- **Persistence:** `data/state.json` through Vite middleware (`server/stateFile.ts`)

## Key paths

```
src/
  App.tsx, budget.ts, persistence.ts
  hooks/useBudgetState.ts
  components/PotAlert.tsx, TeamMemberEditor.tsx
server/stateFile.ts          # GET/PUT /api/state
data/state.json              # live data (snapshot in backup/)
vite.config.ts               # statePersistencePlugin
prototype/NOTES.md           # Variant A verdict (archived)
```

## Run

```bash
npm install
npm run dev
```

API: `GET/PUT http://localhost:5173/api/state`

## Snapshots in this folder

| File | Description |
|------|-------------|
| `PRODUCT.md` | Product spec |
| `README.md` | Run instructions |
| `NOTES.md` | Prototype verdict |
| `budget.ts` | From `src/budget.ts` |
| `state.json` | From `data/state.json` |

## Restore commands

```bash
cp backup/PRODUCT.md PRODUCT.md
cp backup/README.md README.md
cp backup/NOTES.md prototype/NOTES.md
cp backup/budget.ts src/budget.ts
cp backup/state.json data/state.json
```

Then `npm install && npm run dev`.

## Delta since restore-point-2026-05-21T115542Z

1. Promoted Variant A to root `src/` (production app)
2. Removed `prototype/ui/`
3. Added JSON persistence (`server/stateFile.ts`, `src/persistence.ts`, debounced save in hook)
4. Added `data/state.json` + example template
