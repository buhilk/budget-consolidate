# Restore point — budget-consolidate (post-prototype)

| Field | Value |
|-------|--------|
| **Created** | 2026-05-21T11:55:42Z |
| **Project** | `budget-consolidate` |
| **Phase** | Requirements + **UI prototype complete**; **Variant A chosen**; production app not promoted yet |
| **Prior restore** | [restore-point-2026-05-21.md](./restore-point-2026-05-21.md) (requirements only) |

## Status at this point

| Done | Not done |
|------|----------|
| PRODUCT.md fenced (7k LKR, 98% warn, allocation-only, central pot UX) | Production Vite app at repo root |
| Throwaway UI prototype (`prototype/ui`, Vite 5) | Delete B/C variants + switcher |
| **Design winner: Variant A (Hero pot)** in `prototype/NOTES.md` | Persistence / auth |
| Portable logic in `prototype/ui/src/budget.ts` | Promote A to real app |

## Locked decisions (unchanged from prior restore)

- `allocation(team) = headcount × 7_000 LKR`
- Central used = Σ allocations; warn-only; near-limit **≥ 98%**; 100% pot OK; over = danger + overflow meter

## Prototype verdict

**Winner: A — Hero pot**

- Central pot strip on top (meter, figures, alerts)
- Team cards grid below with member entry
- Fold from `prototype/ui/src/budget.ts`; drop B/C + `PrototypeSwitcher` when promoting

Run prototype: `cd prototype/ui && npm run prototype` → `?variant=A`

## File snapshots in this folder

| File | Description |
|------|-------------|
| `PRODUCT.md` | Product spec |
| `README.md` | Project readme |
| `NOTES.md` | Prototype notes + verdict |
| `budget.ts` | Portable budget logic (from `prototype/ui/src/budget.ts`) |

## Restore commands

```bash
# Docs + spec
cp backup/PRODUCT.md PRODUCT.md
cp backup/README.md README.md
cp backup/NOTES.md prototype/NOTES.md

# Budget logic (if promoting from backup)
cp backup/budget.ts prototype/ui/src/budget.ts
```

## Delta since first restore (2026-05-21)

1. Added `prototype/` UI with variants A, B, C
2. Fixed Vite module resolution (logic lives under `prototype/ui/src/`)
3. Recorded **Variant A** as layout winner
4. README links prototype + winner

## Next step (when ready)

Promote Variant A to a non-throwaway app at repo root (or `app/`), keep `budget.ts` logic, implement per PRODUCT.md acceptance criteria.
